# Flask Configuration
SECRET_KEY=your-secret-key-here-change-in-production

# Google Gemini AI API (Required for AI-powered syllabus parsing)
GEMINI_API_KEY=your-gemini-api-key-here

# Flask Environment (development/production)
FLASK_ENV=production

# Port (Render will set this automatically)
PORT=5000

# OAuth Configuration (not needed in production with HTTPS)
# OAUTHLIB_INSECURE_TRANSPORT=1

# Note: You also need to place your Google OAuth credentials.json file
# in the project root directory. This file is obtained from Google Cloud Console.
